function main() {
    let myEmail: string = "cb473355@gmail.com";
    console.log(myEmail);
};

main();

// para transcrever para javaScript usar npx tsc src/index.ts
// criar arquivo tsconfig para mudar tudo de uma vez npx tsc --init