const devArea = {
    version: "01ab",
    production: true,
};

const client = {
    device: "web",
};

module.exports = {
    devArea,
    client,
};


// Valor imutável: Uma variável declarada com const não pode ser 
// reatribuída (ou seja, não pode receber um novo valor depois de 
// sua declaração